services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flenski-frontend
    ports:
      - "5173:5173"
      - "5174:5174"
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development
    stdin_open: true
    tty: true
    networks:
      - flenski-network

networks:
  flenski-network:
    driver: bridge